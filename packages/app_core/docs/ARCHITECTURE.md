# Architecture & Module Relationships

## 📊 Complete Module Dependency Graph

```
┌─────────────────────────────────────────────────────────────────┐
│                      APP CORE PACKAGE                           │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                 setupAppCoreDI() [DI]                    │  │
│  │         One-time initialization in main()               │  │
│  └──────────────────────────────────────────────────────────┘  │
│              ↓           ↓              ↓           ↓            │
│         Creates:   Registers:   Configures:  Uses:             │
│         ┌───────┐  ┌────────┐  ┌────────┐  ┌──────────┐       │
│         │  Dio  │  │HttpCli │  │Interceptor │Constants│       │
│         └───────┘  │ent     │  └────────┘  └──────────┘       │
│             │      └────────┘      │ │                          │
│             │          │           │ │                          │
│    ┌────────┴──────────┴───────────┤ │                          │
│    │                               │ │                          │
│    ▼                               ▼ ▼                          │
│  ┌──────────────────────────────────────────────────┐          │
│  │         HTTP Module                             │          │
│  │  ┌──────────────────────────────────────────┐   │          │
│  │  │ HttpClient (Interface)                   │   │          │
│  │  │  ├─ DioHttpClient (Implementation)       │   │          │
│  │  │  ├─ Methods: get, post, put, patch, delete   │   │          │
│  │  │  └─ Returns: Response (raw)             │   │          │
│  │  │                                         │   │          │
│  │  │ DioClientBuilder                         │   │          │
│  │  │  └─ Builds Dio with base config        │   │          │
│  │  │                                         │   │          │
│  │  │ Interceptors                            │   │          │
│  │  │  ├─ AuthInterceptor                     │   │          │
│  │  │  │  ├─ Uses: TokenStorage, AuthHandler │   │          │
│  │  │  │  ├─ Adds Bearer token               │   │          │
│  │  │  │  ├─ Handles 401 (refresh token)    │   │          │
│  │  │  │  └─ Retries on success              │   │          │
│  │  │  │                                      │   │          │
│  │  │  └─ LoggingInterceptor                 │   │          │
│  │  │     └─ Logs requests/responses         │   │          │
│  │  └──────────────────────────────────────────┘   │          │
│  └──────────────────────────────────────────────────┘          │
│              │                                    │              │
│              │ (returns Response)                 │              │
│              │                                    │              │
│              ▼                                    ▼              │
│  ┌──────────────────────────┐    ┌──────────────────────────┐  │
│  │  Error Mapping           │    │  Auth Module             │  │
│  │  ┌──────────────────────┐│    │  ┌──────────────────────┐│  │
│  │  │ DioException         ││    │  │ AuthEventHandler     ││  │
│  │  │  └─ Status code?    ││    │  │ (Interface)          ││  │
│  │  │  └─ Type?           ││    │  │ ├─ refreshTokenReq() ││  │
│  │  │  └─ Extract message ││    │  │ ├─ onParsedNewToken()││  │
│  │  │                      ││    │  │ └─ onSessionExpired()││  │
│  │  │ AppException         ││    │  │                      ││  │
│  │  │ (7 concrete types)   ││    │  │ Implemented by:      ││  │
│  │  │ ├─ NetworkException  ││    │  │  Project-specific    ││  │
│  │  │ ├─ AuthException     ││    │  │  code                ││  │
│  │  │ ├─ ValidationException││    │  └──────────────────────┘│  │
│  │  │ ├─ ServerException   ││    └──────────────────────────┘  │
│  │  │ ├─ NotFoundException ││             │                    │
│  │  │ ├─ ForbiddenException││             │                    │
│  │  │ └─ GenericException  ││             │                    │
│  │  └──────────────────────┘│             │                    │
│  └──────────────────────────┘             │                    │
│              │                             │                    │
│              └─────────────────────────────┘                    │
│                                │                                │
│                                ▼                                │
│  ┌──────────────────────────────────────────────────┐          │
│  │  Storage Module                                 │          │
│  │  ┌──────────────────────────────────────────┐   │          │
│  │  │ TokenStorage (Interface)                 │   │          │
│  │  │ ├─ getToken(): String? (sync)           │   │          │
│  │  │ ├─ saveToken(String): Future (async)    │   │          │
│  │  │ ├─ getRefreshToken(): String?           │   │          │
│  │  │ ├─ saveRefreshToken(String): Future     │   │          │
│  │  │ ├─ clearTokens(): Future                │   │          │
│  │  │ └─ initialize(): Future                 │   │          │
│  │  │                                          │   │          │
│  │  │ DefaultTokenStorage (Implementation)    │   │          │
│  │  │ ├─ Memory cache: _accessToken           │   │          │
│  │  │ └─ Persistent: SecureStorage            │   │          │
│  │  │                                          │   │          │
│  │  │ AppStorage (Interface)                   │   │          │
│  │  │ ├─ setString/getInt/getBool             │   │          │
│  │  │ └─ remove/clear                         │   │          │
│  │  │                                          │   │          │
│  │  │ AppSharedPreferences (Implementation)   │   │          │
│  │  │ └─ Uses SharedPreferences               │   │          │
│  │  └──────────────────────────────────────────┘   │          │
│  └──────────────────────────────────────────────────┘          │
│              │                                                   │
│              └─────────────────────────┐                         │
│                                        │                         │
│  ┌─────────────────────────┐           │  ┌────────────────────┐│
│  │ Constants Module        │           │  │ Helpers Module     ││
│  │ ┌───────────────────────┤           │  │ ┌──────────────────┤│
│  │ │ HttpConstants         │           │  │ │String Extensions ││
│  │ │ ├─ Timeouts           │           │  │ │├─ isValidEmail() ││
│  │ │ ├─ Status codes       │           │  │ │├─ capitalize()   ││
│  │ │ ├─ Headers            │           │  │ │└─ limitLength()  ││
│  │ │ ├─ Response fields    │           │  │ │                  ││
│  │ │ └─ Retry config       │           │  │ │Context Extensions││
│  │ │                       │           │  │ │├─ screenWidth    ││
│  │ │ AppConstants          │           │  │ │├─ isDarkMode     ││
│  │ │ ├─ Upload limits      │           │  │ │├─ showSnackBar() ││
│  │ │ ├─ Cache config       │           │  │ │└─ navigate()     ││
│  │ │ └─ Session config     │           │  │ │                  ││
│  │ └───────────────────────┤           │  │ │DioOptions Ext    ││
│  └─────────────────────────┘           │  │ │├─ withTimeout()  ││
│                                        │  │ │├─ withHeaders()  ││
│                                        │  │ │└─ getValueByPath()││
│                                        │  │ │                  ││
│                                        │  │ │Debouncer         ││
│                                        │  │ │ └─ delay()       ││
│                                        │  │ │                  ││
│                                        │  │ │AppLogger         ││
│                                        │  │ │ └─ info/error()  ││
│                                        │  │ └──────────────────┘│
│                                        │  └────────────────────┘│
│                                        │                         │
│                                        └─────────────────────────│
│                                                                 │
│  ┌──────────────────────────────────────────────────┐          │
│  │  Design System (colors, typography, etc)        │          │
│  │  Widgets (reusable UI components)                │          │
│  └──────────────────────────────────────────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow: Request → Response → Handling

```
┌─────────────────────────────────────────────────────────────────┐
│  1. CLIENT INITIATES REQUEST                                    │
├─────────────────────────────────────────────────────────────────┤
│  Project BLoC/ViewModel:                                        │
│  final httpClient = GetIt.I<HttpClient>();                     │
│  final response = await httpClient.get('/users');               │
└─────────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. HTTP CLIENT PREPARES REQUEST                                │
├─────────────────────────────────────────────────────────────────┤
│  DioHttpClient → Dio.get()                                      │
│                  ↓                                               │
│  Interceptor Chain: onRequest()                                 │
│  ├─ AuthInterceptor                                             │
│  │  ├─ Get token from TokenStorage.getToken() [SYNC]           │
│  │  ├─ Add header: Authorization: Bearer {token}               │
│  │  └─ Pass to next                                            │
│  │                                                              │
│  └─ LoggingInterceptor                                          │
│     ├─ Log: "→ REQUEST: GET /users"                            │
│     └─ Pass to network                                          │
└─────────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. NETWORK LAYER SENDS REQUEST                                 │
├─────────────────────────────────────────────────────────────────┤
│  HTTP Request sent to server                                    │
│  Path: baseUrl + /users                                         │
│  Method: GET                                                    │
│  Headers: Authorization: Bearer abc123...                       │
└─────────────────────────────────────────────────────────────────┘
         │
         ▼ (Server processes)
         │
         ├─ SUCCESS (200-299)          │ ERROR (3xx-5xx)           │
         ▼                              ▼                           │
┌─────────────────────────┐    ┌──────────────────────────┐       │
│  4a. SUCCESS RESPONSE    │    │  4b. ERROR RESPONSE      │       │
├─────────────────────────┤    ├──────────────────────────┤       │
│  Interceptor: onResponse│    │  Interceptor: onError()  │       │
│                         │    │                          │       │
│  LoggingInterceptor     │    │  Status = 401?           │       │
│  └─ Log: "← RESPONSE: 200"   │  YES → AuthInterceptor   │       │
│                         │    │   ├─ refreshTokenRequest()│       │
│  Return Response        │    │   ├─ save new token      │       │
│  ├─ statusCode: 200     │    │   ├─ onParsedNewToken()  │       │
│  ├─ data: [{...}, {...}]    │   └─ Retry original request       │
│  └─ headers: {...}      │    │                          │       │
└─────────────────────────┘    │  Status != 401           │       │
         │                      │  └─ mapDioException()   │       │
         │                      │     └─ Throw AppException        │
         │                      └──────────────────────────┘       │
         │                              │                         │
         ▼                              ▼                         │
┌──────────────────────────────────────────────────────────────┐  │
│  5. CALLER HANDLES RESPONSE OR EXCEPTION                      │  │
├──────────────────────────────────────────────────────────────┤  │
│                                                              │  │
│  try {                                                       │  │
│    final response = await httpClient.get('/users');        │  │
│                                                              │  │
│    // Success path                                           │  │
│    if (response.isSuccessful) {                             │  │
│      final users = response.dataAsList;  [HELPER]           │  │
│      return users.map(...).toList();                        │  │
│    }                                                         │  │
│                                                              │  │
│  } on ValidationException catch (e) {                       │  │
│    // Handle validation error with field errors             │  │
│    showFieldErrors(e.fieldErrors);                          │  │
│                                                              │  │
│  } on AuthException catch (e) {                             │  │
│    // Handle auth error                                      │  │
│    context.showSnackBar(e.message);  [HELPER]              │  │
│    context.go('/login');  [HELPER]                          │  │
│                                                              │  │
│  } on NetworkException catch (e) {                          │  │
│    // Handle network error                                   │  │
│    showOfflineMessage();                                     │  │
│                                                              │  │
│  } on ServerException catch (e) {                           │  │
│    // Handle server error                                    │  │
│    context.showError('Server Error', e.message);  [HELPER]  │  │
│                                                              │  │
│  } on AppException catch (e) {                              │  │
│    // Fallback for any other exception                       │  │
│    emit(UserError(e.message));                              │  │
│  }                                                            │  │
│                                                              │  │
└──────────────────────────────────────────────────────────────┘  │
         │                                                        │
         ▼                                                        │
┌──────────────────────────────────────────────────────────────┐  │
│  6. PROJECT ADAPTER LAYER (OPTIONAL)                          │  │
├──────────────────────────────────────────────────────────────┤  │
│  Map raw response to domain models                           │  │
│                                                              │  │
│  Response.data → User object → BLoC/ViewModel              │  │
│                                                              │  │
│  class UserAdapter {                                         │  │
│    final httpClient = GetIt.I<HttpClient>();               │  │
│                                                              │  │
│    Future<List<User>> getUsers() async {                    │  │
│      try {                                                   │  │
│        final response = await httpClient.get('/users');    │  │
│        final json = response.dataAsList;                    │  │
│        return json.map((j) => User.fromJson(j)).toList(); │  │
│      } on AppException catch (e) {                          │  │
│        rethrow;                                              │  │
│      }                                                        │  │
│    }                                                          │  │
│  }                                                            │  │
│                                                              │  │
└──────────────────────────────────────────────────────────────┘  │
         │                                                        │
         ▼                                                        │
┌──────────────────────────────────────────────────────────────┐  │
│  7. UI LAYER DISPLAYS RESULT                                 │  │
├──────────────────────────────────────────────────────────────┤  │
│  BLoC emits state                                            │  │
│  ├─ UserSuccess(users) → Build ListView                     │  │
│  ├─ UserError(message) → Show error message                 │  │
│  └─ UserLoading() → Show spinner                            │  │
│                                                              │  │
└──────────────────────────────────────────────────────────────┘  │
```

---

## 📋 Module Initialization Order

```
1. main()
   ├─ Initialize TokenStorage
   │  └─ await tokenStorage.initialize()
   │     └─ Load tokens from SecureStorage into memory
   │
   ├─ setupAppCoreDI()  ← MUST come after storage init
   │  ├─ Create DioClientBuilder
   │  ├─ Build Dio instance
   │  ├─ Add AuthInterceptor (uses TokenStorage)
   │  ├─ Add LoggingInterceptor
   │  ├─ Register HttpClient
   │  ├─ Register all services in GetIt
   │  └─ Everything ready for injection
   │
   └─ runApp()
      └─ BLoCs can now GetIt.I<HttpClient>()
```

---

## 🔐 Security & Privacy Boundaries

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURE BOUNDARY                          │
│  (Token, AuthHandler, SessionState)                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────┐        ┌───────────────────┐         │
│  │ TokenStorage     │        │ AuthEventHandler  │         │
│  │ ├─ Tokens        │        │ ├─ Refresh logic  │         │
│  │ │  SecureStorage │        │ ├─ Session expire │         │
│  │ └─ Memory cache  │        │ └─ Logout         │         │
│  └──────────────────┘        └───────────────────┘         │
│          │                             │                    │
│          └─────────────────────────────┘                    │
│                    │                                        │
│                    ▼                                        │
│          AuthInterceptor [HTTP]                            │
│          ├─ Adds token to header                           │
│          ├─ Detects 401                                    │
│          ├─ Calls refresh                                  │
│          └─ Persists new token                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘

          │ Exposed (Public API)
          ▼

┌─────────────────────────────────────────────────────────────┐
│                   PUBLIC BOUNDARY                           │
│  (HttpClient, Errors, Helpers)                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ HttpClient   │  │ AppException │  │ Extensions   │     │
│  │ ├─ get()     │  │ (7 types)    │  │ ├─ String    │     │
│  │ ├─ post()    │  │              │  │ ├─ Context   │     │
│  │ └─ delete()  │  │              │  │ └─ DioOptions│     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│       │                    │                  │             │
│       └────────────────────┴──────────────────┘             │
│                    │                                        │
│                    ▼                                        │
│          Project BLoC / ViewModel                           │
│          ├─ Uses HttpClient for API calls                  │
│          ├─ Catches AppException by type                   │
│          └─ Uses helpers for UI                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Design Principles

### 1. **Separation of Concerns**
```
Storage Module  → Manages persistence
HTTP Module     → Manages network
Auth Module     → Defines auth contract
Errors Module   → Standardizes exceptions
DI Module       → Manages dependencies
Helpers         → Provides utilities
```

### 2. **Dependency Inversion**
```
High-level (Project) depends on Low-level (app_core) via Interfaces
├─ HttpClient interface (not DioHttpClient)
├─ TokenStorage interface (not DefaultTokenStorage)
├─ AuthEventHandler interface (project implements)
└─ AppException abstract (concrete subclasses)
```

### 3. **Single Responsibility**
```
TokenStorage    → Only manages tokens
HttpClient      → Only handles HTTP
AuthInterceptor → Only handles 401 & token refresh
ErrorMapper     → Only maps exceptions
```

### 4. **Loose Coupling**
```
Modules don't directly depend on each other
├─ HTTP doesn't import Auth
├─ Auth doesn't import Storage
├─ DI orchestrates all via interfaces
└─ Project implements contracts
```

---

## 📊 Request Lifecycle with Timestamps

```
T+0ms    Client calls httpClient.get('/users')
T+5ms    DioHttpClient → Dio
T+10ms   AuthInterceptor.onRequest()
         ├─ TokenStorage.getToken() [0.1ms]
         └─ Add header
T+15ms   LoggingInterceptor logs request
T+20ms   HTTP request sent to server
T+500ms  Server processes (imagine delay)
T+750ms  Response received: 200 OK

         AuthInterceptor.onResponse()
         └─ Not 401, pass through
T+755ms  LoggingInterceptor logs response
T+760ms  Return to caller
T+770ms  BLoC/ViewModel processes response
T+790ms  UI updates with data
```

---

## 🔗 Module Import Order

**What to import where:**

```dart
// lib/features/home/presentation/bloc/home_bloc.dart
import 'package:app_core/app_core.dart';  // Everything you need

// main.dart
import 'package:app_core/app_core.dart';

// lib/features/user/data/adapters/user_adapter.dart
import 'package:app_core/app_core.dart';  // HttpClient, AppException, GetIt

// lib/features/auth/presentation/pages/login_page.dart
import 'package:app_core/app_core.dart';  // Context extensions, exceptions

// Widget that needs responsive design
import 'package:app_core/app_core.dart';  // Context extensions
```

---

## 🚀 Extension Points

Where projects customize app_core:

```
1. AuthEventHandler [REQUIRED]
   └─ Implement in project
      ├─ refreshTokenRequest() - Call your refresh endpoint
      ├─ onParsedNewToken() - Update UI, resume requests
      └─ onSessionExpired() - Logout, navigate

2. Error Mapper [OPTIONAL]
   └─ Extend for custom error types
      ├─ RateLimitException
      ├─ MaintenanceException
      └─ Custom business errors

3. Custom Interceptors [OPTIONAL]
   └─ Add to Dio for special handling
      ├─ Analytics tracking
      ├─ Request signing
      └─ Custom headers

4. Adapter Layer [REQUIRED]
   └─ Implement for each domain
      ├─ Map HttpClient → Domain models
      ├─ Handle AppException types
      └─ Implement business logic
```

---

**Navigation**:
- [Main README](./README.md) - Quick start
- [Auth Module](./AUTH.md) - Detailed auth setup
- [Storage Module](./STORAGE.md) - Token management
- [HTTP Module](./HTTP.md) - Network layer
- [Errors Module](./ERRORS.md) - Exception handling
- [Constants Module](./CONSTANTS.md) - Configuration
- [Helpers](./HELPERS.md) - Utilities
- [DI Setup](./DI.md) - Dependency injection
